# 教务管理系统 v3.0 开发计划（规范版 · 需求可追踪）

> 版本：v3.0 · 2026-08-04 · 来源：REQUIREMENTS.md（需求清单）+ 双端现状实测
> 定位：新仓库 `项目根目录` 主开发计划。每项任务 = 需求编号 + 产出文件 + 测试 + 验收，可追踪、可勾选。
> 状态图例：⬜待做 ｜ 🟡进行中 ｜ ✅完成 ｜ ❌取消

---

## 0. 总览

- **第 0 条铁律（最高优先）**：**不重复造轮子**。每项任务开始前**必须先搜索**是否已有成品可用——特别是 GitHub 高质量开源项目，能直接拿来用（pip/组件/整模块）或改造后用，绝不从零写。搜索优先级：GitHub > PyPI > 本地旧项目资产 > 自研。
- **目标**：基于现有系统重构零敏感干净仓库，推送 GitHub 公开；模块化配置化重构；277 测试基线不降。
- **里程碑**：M0 准备冻结 → M1 新仓库 → M2 模块化重构 → M3 加固验收 → M4 公开上线 → M5 待办补齐 → M6 后续 Sprint。
- **双端**：桌面 PyQt5=主 UI；API FastAPI=服务层；**Web 前端=明确需求（G2-G5，双端界面一致，非 YAGNI）**。
- **规范**：语义化版本 / 约定式提交 / GitHub Flow / git-cliff / TDD / 迁移四步曲 / DoD。
- **借鉴原则**：旧项目及网上成熟项目的组件/模块**可借鉴参考，但必须以本项目的开发原则为主**（配置驱动/分层单向依赖/插件式注册/零代码扩展）——借鉴只解决"怎么做"，不改变"做成什么样"。
- **开发纪律（硬性）**：**每轮开发最大 150 步**；禁止超大任务，小步快进，每步走稳；**重视功能复用、减少代码量**（已有服务层先行复用，新增先搜现有组件，绝不重复造轮子）。

---

## M0：准备与冻结（0.5 天）

| 任务 | 需求 | 产出 | 验收 |
|------|------|------|------|
| M0-1 旧仓库冻结 | 原则6 | README 顶部 FROZEN 标注 | 停止向 master 推送 |
| M0-2 复核整合分支 | — | integrate-clean 验证 | 277 passed + 零残留 |
| M0-3 排除项核验 | 原则6 | 排除清单确认 | 73400340/data/logs/import_real_data/templates/static 空目录 |

---

## M1：新仓库搭建（0.5 天）

| 任务 | 需求 | 产出 | 验收 |
|------|------|------|------|
| M1-1 git init + 复制源码 | 原则1-2 | edu-management + 首次 commit | 排除清单核对 |
| M1-2 重建 venv + 依赖 | 规范6 | requirements.lock 校验 | 依赖可复现 |
| M1-3 冒烟验收 | — | pytest + main.py --help | **277 passed** + 正常退出 |

---

## M2：模块化配置化重构（2-3 天，每步 TDD）

| 任务 | 需求 | 产出文件 | 测试目标 | 验收 |
|------|------|----------|----------|------|
| M2-1 配置单源化 | 原则6 | config.py→config/__init__.py | test_smoke/test_ui_config | 无 config.py、双源消除 |
| M2-2 死代码清理 | 原则6 | 删 navigation/design_system/templates/static | grep 零引用 | 无残留引用 |
| M2-3 API 补 /api/config | 双端定位 | api/routes + deps | +2 契约测试 | API 暴露 ui_config |
| M2-4 cache 去重+依赖精简 | 规范6 | requirements.lock | pytest 全绿 | 无未用包 |
| M2-5 文档重写 | 规范7 | README/CHANGELOG | 文档审阅 | 公开友好 |
| M2-6 CI 适配 | 规范3 | .github/workflows/* | CI 触发 | 公开 runner |

---

## M3：加固验收（1 天）

| 任务 | 需求 | 产出 | 验收 |
|------|------|------|------|
| M3-1 敏感扫描 | 原则6 | grep 结果 | 校名关键词零命中 + git log 零敏感 |
| M3-2 全量测试+格式 | 规范5 | pytest/ruff | 277 passed + All checks passed |
| M3-3 安全扫描 | 规范8 | bandit/gitleaks | 0 高危 |
| M3-4 空库迁移演练 | 规范6 | alembic | upgrade head 全表一致 |
| M3-5 Windows dry-run | Sprint7 | CI build job | 构建通过 |

---

## M4：公开上线（0.5 天）

| 任务 | 需求 | 产出 | 验收 |
|------|------|------|------|
| M4-1 建公开仓库 | — | gh repo create --public | 推送成功 |
| M4-2 CI 全绿 | 规范3 | 9 jobs | 全绿 |
| M4-3 Release | 规范4 | 徽章+首版 | 可下载 |
| M4-4 旧仓库归档 | 原则6 | 转 private | 观察 1 个月 |

---

## M5：待办缺口（按优先级，逐项 TDD）

> 对应 REQUIREMENTS 第三节 A-F 组，按依赖排序。每项完成即勾选+commit。

### M5-A 学期上下文（需求 A1-A5）
| 任务 | 需求 | 产出文件 | 测试 | 验收 |
|------|------|----------|------|------|
| M5-A1 会话上下文管理器 | A1 | database.py | +2 单测 | 线程局部+请求级 |
| M5-A2 before_compile 注入 | A2 | database.py | +3 单测 | 排除系统表 |
| M5-A3 FastAPI 依赖注入 | A3 | api/deps.py | +2 契约 | 双端复用 |
| M5-A4 学期切换器 UI | A4 | semester.py+main_window | GUI 测试 | 广播刷新 |
| M5-A5 学期维度权限 | A5 | core/permissions.py | +3 单测 | 细粒度 |

### M5-B 统计预计算（需求 B1-B5）
| 任务 | 需求 | 产出文件 | 测试 | 验收 |
|------|------|----------|------|------|
| M5-B1 核心指标清单 | B1 | statistics.py | +5 单测 | 30 指标 |
| M5-B2 增量刷新 | B2 | statistics.py+events | +3 单测 | 事件驱动 |
| M5-B3 后台 Worker | B3 | statistics_worker.py | +2 单测 | 进度/取消 |
| M5-B4 手动触发 | B4 | system_config+admin | +2 契约 | 幂等 |
| M5-B5 缓存 API + 304 | B5 | stats.py | +2 契约 | 无实时聚合 |

### M5-C 配置继承与锁定（需求 C1-C4）
| 任务 | 需求 | 产出文件 | 测试 | 验收 |
|------|------|----------|------|------|
| M5-C1 继承向导 UI | C1 | semester.py | GUI 测试 | 四色预览 |
| M5-C2 配置版本回滚 | C2 | models | +2 单测 | 软删除+version |
| M5-C3 锁定 UI | C3 | base.py | GUI 测试 | 权限控制 |
| M5-C4 典型锁定场景 | C4 | score/enrollment | +4 单测 | 自动加锁 |

### M5-D 桌面补全（需求 D1-D6）
| 任务 | 需求 | 产出文件 | 测试 | 验收 |
|------|------|----------|------|------|
| M5-D1 LoginDialog | D1 | gui/dialogs/login.py | GUI 测试 | 键盘全流程 |
| M5-D2 导入向导 UI | D2 | gui/views/import_wizard.py | GUI 测试 | 3000 人<2 分钟 |
| M5-D3 列持久化/密度 | D3 | components.py | 单测 | QSettings |
| M5-D4 状态栏局域网 | D4 | main_window.py | GUI 测试 | ServerThread 接入 |
| M5-D5 报表模板管理 | D5 | report_template.py | 单测 | 版本管理 |
| M5-D6 批量生成 Worker | D6 | report_worker.py | 单测 | 500 份<30s |

### M5-E 业务 API 补全（需求 E1-E7）
| 任务 | 需求 | 产出文件 | 测试 | 验收 |
|------|------|----------|------|------|
| M5-E1 成绩录入增强 | E1 | score.py | +3 契约 | 粘贴/排名/锁定 |
| M5-E2 考勤增强 | E2 | attendance.py | +3 契约 | 推送/离线 |
| M5-E3 学生查分 | E3 | 新路由 | +2 契约 | 仅本人+趋势 |
| M5-E4 班级名单 | E4 | 新路由 | +2 契约 | 只读+导出 |
| M5-E5 配置继承 API | E5 | 新路由 | +2 契约 | 四色预览 |
| M5-E6 数据锁定 API | E6 | 新路由 | +2 契约 | 批量+理由 |
| M5-E7 导入导出 API | E7 | 新路由 | +3 契约 | 模板下载 |

### M5-F 服务管理与集成（需求 F1-F5）
| 任务 | 需求 | 产出文件 | 测试 | 验收 |
|------|------|----------|------|------|
| M5-F1 服务管理 UI | F1 | system_config.py | GUI 测试 | 实时生效 |
| M5-F2 端到端验证 | F2 | 集成 | 压测 | <3 秒 |
| M5-F3 并发压测 | F3 | 压测脚本 | 20 设备 | WAL 无锁死 |
| M5-F4 优雅停机 | F4 | server_thread | 集成 | 无残留进程 |
| M5-F5 6 域导航验收 | F5 | 集成 | 全绿 | 角色过滤 |

### M5-G 双端一致 + 快捷验收（需求 G1-G5，用户 2026-08-04 补充）
| 任务 | 需求 | 产出文件 | 测试 | 验收 |
|------|------|----------|------|------|
| M5-G1 桌面快捷方式 | G1 | scripts/install_desktop.sh + .desktop | 安装验证 | 桌面双击直达主界面 |
| M5-G2 Web 前端骨架 | G2 | static/ + Jinja2 模板（base.html 渲染 ui_config 6 域） | 浏览器冒烟 | / 返回完整界面 |
| M5-G3 双端一致渲染 | G3 | 两端共用 ui_config 渲染器 | 视觉对照 | 品牌/导航/页签/主题一致 |
| M5-G4 配置热加载 | G4 | ui_config 监听 | 单测 | 改配置两端即生效 |
| M5-G5 Web 认证一致 | G5 | Web 登录页复用 auth API | 契约测试 | 同一账号两端通行 |

> **双端一致设计**：ui_config.json 是唯一配置源；桌面 PyQt5 与 Web Jinja2 各自实现渲染器，读同一配置 → 视觉一致。Web 端只做 6 域导航 + 页签框架 + 登录，业务页复用现有 API（无构建工具，CDN Alpine.js/Tabulator）。

---

## M6：后续 Sprint（v3.1+，不阻塞公开）

> **范围边界（EDU_REVIEW 教务审视定案，2026-08-04）**：本系统**适配小型学校，不追求宏大**。以下为剪裁决策：

| 原 Sprint 项 | 决策 | 理由 |
|-------------|------|------|
| 排课引擎（OR-Tools） | ❌ **砍** | 小型学校固定课表，复杂度>>收益 |
| 家长多通道推送（微信/短信） | ❌ **砍** | 个人作品无通道资源；如要只做邮件单通道 |
| 教师绩效考核/请假审批流 | ⏸ **延后**（不设里程碑） | 非教务核心 |
| 多校区/离线同步/无障碍全量 | ❌ 已砍 | 单校区/桌面为主/低优先 |

**保留的后续方向（按价值排序）**：
| 阶段 | 内容 | 价值 |
|------|------|------|
| Sprint 5（保留） | 考试管理：分考场/座位/监考/准考证 | ★★★★ 学期大活 |
| Sprint 6（精简） | 学期管理 UI / 报表模板管理 / 批量打印 | ★★★★ 期末刚需 |
| Sprint 7（精简） | Win 打包/签名/更新 + 兼容性收尾 | ★★★★ 部署必须 |

> **小型学校定位一句话**：核心闭环（学生/成绩/学籍/考试/报表）打磨到极致，配置驱动零代码扩展，桌面为主、Web 只读为辅。

---

## 验收基线（DoD，公开前强制）

| 项 | 标准 |
|----|------|
| 全量测试 | 277 passed（不降） |
| 敏感扫描 | grep 校名关键词 零命中 + git log 零敏感 |
| ruff/format | 全绿 |
| 安全扫描 | bandit/gitleaks 0 高危 |
| 迁移演练 | 空库 alembic upgrade head 全表一致 |
| 冒烟 | main.py --help 正常 |
| CI | 9 jobs 全绿（公开免费 runner） |
| 文档 | README + CHANGELOG + REQUIREMENTS + 本计划 |

---

## 开发顺序（业界标准：价值优先 + 依赖驱动 + TDD）

> 业界排序三原则，按此决定 M0-M6 及 M5 各组执行顺序：

| 原则 | 含义 | 落到本项目 |
|------|------|-----------|
| **1. 价值优先 / MoSCoW** | 先做"必须有"(Must) 再做"应该有"(Should)，可延后"可有可无"(Could) | M0-M4 公开上线=Must；M5 待办补齐=Should；M6 后续 Sprint=Could |
| **2. 依赖驱动 / 拓扑序** | 无依赖的先做，被依赖的底座先做 | 配置单源(M2-1)→死代码清理(M2-2)→API 层(M2-3)；学期底座(A)→统计(B)→锁定(C) |
| **3. 风险先行 / 最低可行** | 高风险/不确定性先验证，避免返工 | M3 加固验收提前；Web 前端(YAGNI)最末 |

**逐任务开发循环（每项都走）**：
```
0. 搜索成品（第 0 条铁律）：GitHub/PyPI/本地资产 → 有则拿来用或改造
1. 写失败测试 (RED) → 2. 跑测试确认失败 → 3. 实现最小代码 (GREEN)
→ 4. 跑测试确认通过 → 5. 重构优化 → 6. commit + CI 绿
```

**依赖图（M5 组间顺序）**：
```
M5-A 学期上下文 ──► M5-B 统计预计算（依赖 A 的学期上下文）
      │
      └──────────► M5-C 配置继承（依赖 A）
M5-D 桌面补全（服务层已就绪，可与 A/B/C 并行）
M5-E 业务 API（依赖 A 的学期上下文）
M5-F 服务管理+集成（依赖所有以上）
```

---

## 工作接续（业界标准：文档驱动，不依赖记忆）

> **核心问题**：你"零碎靠我开发，对话框关闭一切归零"。业界禁止靠对话记忆接手——靠**持久文档 + git 状态 + 自动化**三件套。每个会话独立可续。

### 1. 接手五步（任何新对话第一件事）
```
1. 读 REFACTOR_PLAN.md + DEV_PLAN_v3.md → 当前里程碑/任务
2. git status + git log --oneline -5 → 未提交改动/最近进度
3. 跑测试基线 → 确认健康（fresh pytest，不接受过期快照）
4. 看 TODO.md / 本文件「变更追踪」→ 续接点
5. 按 DEV_PLAN_v3 执行下一任务 → 完成即更新状态
```

### 2. 每会话结束写「接续点」（必做）
在 `DEV_PLAN_v3.md` 底部「会话接续记录」追加：
```markdown
## 会话接续记录
| 日期 | 当前分支 | 完成到 | 下一步 | 未提交改动 |
|------|----------|--------|--------|-----------|
| 08-04 | integrate-clean | 脱敏半成品 | commit 后合入 | 11 文件 M |
```

### 3. 防"归零"纪律
- **每完成一个原子任务立即 commit**（不攒批量）→ git log 是永不丢的记忆
- **改动多分支**用「任务完成记录模板」留痕（旧仓库 §487）
- **测试基线写死**在文档（不用快照，fresh 跑）
- 任何"我记忆里…"的说法，**一律以 git/文档为准**，不靠对话

### 4. 业界工具辅助（可选提升）
- `session_search`：本地跨会话检索旧对话上下文
- GitHub Issues：每个待办转 Issue，跨设备/跨人可追踪
- `git stash` + `git stash pop`：临时切任务不丢改动

---

## GitHub 安全与高效使用（公开仓库版）

> 前提变更：仓库从 private 转 public 后，**默认信任模型翻转**——全世界可见，CI 触发更频繁，口径必须收紧。

### 1. 仓库安全基线（公开前必设）
| 项 | 设置 | 目的 |
|----|------|------|
| 分支保护 | `main` 必须 PR + CI 绿 + 1 review | **禁止 `--admin` 直合并** |
| Secret scanning | Push protection | 防 token/密钥入库 |
| Dependabot | Security+Version weekly | 自动修依赖漏洞 |
| CodeQL | Python 静态分析 | 找安全漏洞 |
| 环境密钥 | 存 GitHub Secrets | 不写进代码/配置 |
| .gitignore 加固 | 补 data/密钥/xlsx | 防误提交 |

### 2. 合并流程规范（替代 `--admin`）
```
用户本地 → 开 feature 分支 → commit → push → 开 PR
→ CI 全绿（自动审核） → 你在交互式确认 → gh pr merge --squash
```
> 公开仓库 + 分支保护后，`--admin` 会被拒。改用规范化 PR 流程——**这反而更安全**（CI 强制，防缺陷引入）。

### 3. 免费资源充分利用（已验证 2026-08）
| 资源 | 免费额度 | 本项目用途 |
|------|----------|-----------|
| **Actions（公开仓库）** | **标准 runner 无限分钟** ✅ | CI 9 jobs 全跑，不烧额度 |
| Actions（私有） | 2000 分/月 | 旧仓库冻结期够用 |
| Codespaces | 120 核时/月 + 15GB | 云端开发备选 |
| Pages | 免费托管 | Sphinx 文档站 |
| Packages | 免费存储 | 内部 PyPI |
| Projects/Discussions/Issues | 免费 | 需求看板/决策沉淀 |

### 4. 高效开发节奏（公开仓库）
- **PR 天空化**：小 PR 频繁合，CI 即时反馈
- **标签驱动发布**：打 tag 自动发 Release（见版本管理）
- **双仓库**：`edu-management` 公开（master 干净）+ 旧仓库私有冻结
- **避免返工**：字段动态增删零代码（已实现），减少改表

---

## 交付物（业界标准：可运行制品 + 文档成套）

> 业界定义"一个交付" = 达到 DoD 的完整产物，不只是代码。每个里程碑/版本产出一套交付物：

| 类别 | 交付物 | 归属阶段 | 验收 |
|------|--------|----------|------|
| **源码** | 干净仓库（git 历史零敏感） | M1/M4 | push 成功 |
| **可运行包** | Windows `.exe`（Nuitka）+ Linux AppImage | M4/后续 Sprint7 | 双击即用 |
| **安装包** | NSIS `.exe`（管理员/服务/防火墙/卸载） | Sprint 7 | 卸载干净 |
| **数据库** | alembic 迁移链 + 空库脚本 | M1/M3 | upgrade head 全表一致 |
| **测试证据** | pytest 报告（277+）+ 覆盖率 + 契约测试 | 每任务 | CI 绿 |
| **API 文档** | OpenAPI 3.1（FastAPI 内置 /api/docs） | M2/M3 | 可交互试用 |
| **用户文档** | README + 快速开始 + 配置指南 | M2 | 公开友好 |
| **维护文档** | CHANGELOG + DEV_PLAN_v3 + REQUIREMENTS | 持续 | 追踪可查 |
| **发布 Notes** | git-cliff 生成 Release Notes | 每版本 | 附 GitHub Release |
| **安全报告** | bandit/gitleaks 扫描 0 高危 | M3 | 0 高危 |

**发布节奏（业界惯例）**：
- **MVP 公开**（v3.0.0 MVP）：M1-M4 达成即可公开，即使功能未全
- **迭代版**（v3.1.x）：每完成 M5 一组功能（A/B/C/D/E/F）发一个 MINOR
- **补丁版**（v3.1.1）：Bug 修复即时发 PATCH
- **绝不公开半成品**：DoD 未达不发版

---

## 工程交付自动化（业界标准：PR + CI/CD + 产物 + 许可证 一体化）

> 业界把 PR 流程、自动构建、生成物管理、许可证合规、质量门禁**当作一个整体**设计，不逐项挤牙膏。以下即标准套餐，全部随新项目落地。

### 1. PR 策略（个人开发者也要用 PR）

| 项 | 标准做法 |
|----|----------|
| PR 用途 | ① 触发 CI 自动跑 ② 保持 master 历史整洁（squash）③ 出问题只动 feature 分支 |
| 个人与团队区别 | 无需多人 review；你（或 AI）是唯一审批者，CI 全绿=通过 |
| 标准工作流 | `checkout -b feat/x` → TDD 开发 → commit → `push -u origin feat/x` → `gh pr create` → CI 全绿 → `gh pr merge --squash` → 删分支 |
| 保护分支 | master 必须 PR + required check `test` 绿 + 禁直 push |

### 2. CI/CD 自动化（现有 8 jobs + 补许可证）

| job | 触发 | 产物 | 标准 |
|-----|------|------|------|
| lint（ruff/format） | push/PR | — | 0 error |
| test（全量 277+） | push/PR | pytest-reports(junit.xml) | 全绿 required |
| test-contract | push/PR | — | 契约绿 |
| db-migrate | push/PR | — | 空库迁移一致 |
| test-gui | push/PR | — | GUI 冒烟 |
| security（bandit/pip-audit/semgrep） | push/PR | security-reports | 0 高危 |
| **license（pip-licenses）** | push/PR | THIRD_PARTY.md | 无 GPL 传染/无商用违规 |
| build-windows | main+tag | edu-management-win.exe | 构建绿 |
| build-linux | main+tag | edu-management-linux | 构建绿 |

### 3. 开发生成物标准化（可复现）

| 生成物 | 触发生成 | artifact 名 | 可溯源性 |
|--------|----------|-------------|----------|
| 测试报告 | 每次 CI | pytest-reports | junit.xml + commit SHA |
| 覆盖率 | 每次 CI | coverage-report | 阈值达标 |
| 安全报告 | 每次 CI | security-reports | 0 高危 |
| exe / build | main+tag | 见上 | 带版本/构建号 |
| Release 包 | tag v* | Release assets | 附 CHANGELOG |

**可复现原则**：每次 CI 从干净环境重建，不依赖本地缓存；产物带 commit 标识可溯源到源码。

### 4. 许可证合规 + 致谢（引入第三方必做）

> 第 0 条铁律引入大量第三方组件，必须确认许可证允许商用 + 致谢列明。这是公开仓库法律底线。

| 文件 | 内容 | 时机 |
|------|------|------|
| `LICENSE` | 本项目许可证（建议 MIT 或 Apache-2.0） | 公开前选定 |
| `THIRD_PARTY.md` | 全部第三方依赖：库/版本/许可证/归属 | 每次引入新依赖更新 |
| `pyproject.toml` | `license` 字段 | 公开前设 |
| pip-licenses 检测 | CI 自动生成 + 违规 gate | 每次 PR |

**第三方组件许可证矩阵（引入前核，沿用已验证结论）**：
PyQt-Fluent-Widgets(GPLv3·仅import不传染) / Tabulator(MIT) / Alpine.js(MIT) / ECharts(Apache-2.0) / docxtpl(MIT) / WeasyPrint(BSD) / openpyxl(MIT) / FastAPI(MIT) / passlib(Apache-2.0) — **全部兼容商用**。

**引入新组件前的三步核验**：
```
① 查许可证（GitHub 仓库 LICENSE / PyPI）
② 确认允许商用 + 不改源码不触发传染
③ 记入 THIRD_PARTY.md（若缺失 → pip-licenses 检测失败拦截）
```

### 5. 质量门禁（CI 合并前强制）

- required check：`test` + `lint` + `db-migrate` + `security` + `license`
- 其余（contract/gui/build）作为完整度参考，不阻塞合并
- 任一红 → PR 不能 squash 合并

---

## 版本管理（业界标准：SemVer + 标签化 + 自动化）

> 业界硬规范：版本号唯一可追溯，任何版本可复现、可回滚。全流程自动化。

### 1. 版本号规范（Semantic Versioning）
```
MAJOR.MINOR.PATCH-PRERELEASE+BUILD
例：v3.0.0 · v3.1.0-rc.1 · v3.1.4+a1b2c3d
MAJOR=不兼容变更(改表/删接口)  MINOR=向后兼容新增  PATCH=向后兼容修复
PRERELEASE=alpha/beta/rc      BUILD=git短SHA.时间戳.分支
```

### 2. 版本同步点（三处必须一致）
| 位置 | 文件 |
|------|------|
| 包元数据 | `pyproject.toml` `[project] version` |
| 代码常量 | `src/edu_system/__init__.py` `__version__` |
| 配置显示 | `ui_config.json` `app.version` |

### 3. 分支与发布流（GitHub Flow + 标签）
```
main (受保护，PR 合入，CI 必绿)
  ├─ feature/*  新功能 → PR squash 合入
  ├─ fix/*      缺陷 → PR squash 合入
  └─ 打标签 v3.1.0 → 触发 git-cliff 生成 CHANGELOG + Release
```

### 4. 自动化（git-cliff + GitHub Action）
```
打标签 v* → Action 自动：更新版本号 → 生成 CHANGELOG → 构建制品 → 发布 Release
```

### 5. 版本管理纪律（比代码更严格）
- **提交**：约定式 commit（feat:/fix:/refactor:/docs: 等），git-cliff 依此分组
- **发布**：只从 main 打标签，禁从 feature 直接发
- **回滚**：git revert 发布标签 → 重建 → 打补丁版
- **可追溯**：`git blame` 定位行、`git bisect` 定位回归(5 分钟内)

---

## 数据与业务连续性（业界标准：任何开发不得危及生产数据）

> **核心原则**：重构/开发全程，`data/school_data.db`（282M 真实生产数据）是**唯一不可再生资产**，任何误操作=学校业务灾难。开发数据与生产数据**物理隔离**。

| 项 | 策略 | 验收 |
|----|------|------|
| **开发数据隔离** | 新仓库默认空库/测试库，**绝不指向 data/school_data.db** | 测试全用 tmp/memory |
| **备份频率** | 每次大操作前 `scripts/backup.py` 全量备份（SHA256 校验） | 备份成功日志 |
| **保留策略** | 增量 30 天 + 学期全量 3 年（沿用旧仓库 §3.4.3） | 备份清单可查 |
| **回滚演练** | 空库恢复 → 校验和一致（<10 分钟） | 每 M3 验证一次 |
| **迁移安全** | 改表走 alembic（禁手写 ALTER）+ 空库预演 | CI db-migrate 绿 |
| **灾难恢复** | 旧仓库冻结不动 = 天然备份（观察期 1 个月） | 双仓库并存 |

> **注意**：`data/` 目录 gitignore 且不进新仓库。本地开发用 `test_data` 生成器内存数据或独立 dev.db。

---

## 风险登记册（业界标准：风险显式管理，不靠"小心"）

| # | 风险 | 概率 | 影响 | 缓解 | 状态 |
|---|------|------|------|------|------|
| R1 | 复制源码遗漏文件 | 中 | 高(功能缺失) | M1 后 277 测试 + 清单 diff | ⬜ |
| R2 | 死代码清理误删引用 | 中 | 高(运行时崩溃) | M2-2 先全仓 grep 再删 | ⬜ |
| R3 | 配置合并破坏兼容 | 中 | 中 | M2-1 测试先行 | ⬜ |
| R4 | 生产数据误触 | 低 | **极高** | 数据隔离 + 备份 + 回滚演练 | ⬜ |
| R5 | 公开后误提交敏感 | 中 | 高(声誉) | 扫描 + .gitignore + Secret scanning | ⬜ |
| R6 | CI 公开后仍欠费 | 低 | 中 | 公开仓库 Actions 无限分钟(已验证) | ⬜ |
| R7 | 旧仓库删除前误操作 | 低 | 高(历史丢失) | 冻结 + 观察 1 个月 | ⬜ |
| R8 | PyQt 组件库版本破坏 API | 中 | 中 | 锁版本 + 单测覆盖 | ⬜ |
| R9 | 277 测试基线在重构中退化 | 中 | 中 | 每 M 验收 fresh pytest | ⬜ |

---

## 个人开发者的质量门禁（替代"验收签字"）

> 你是个人开发者，无团队 review、无人签收——**质量靠自动化门禁保证，不靠流程仪式**。每条质量关注点都有可量化的自动检查，CI 强制，绿了才合入。

### 1. 结构合理性（自动化检测）
| 关注点 | 量化指标 | 自动检查 |
|--------|----------|----------|
| 模块边界清晰 | 无循环导入 | `pytest` 导入测试 + `import-linter` 分层规则 |
| 分层正确 | core→api→services→repository→models 单向依赖 | import-linter 契约（禁止反向/跨层） |
| 无死代码 | 删除未引用模块 | 每次清理前 `grep` 全仓引用验证 |

### 2. 模块化与低耦合（自动化检测）
| 关注点 | 量化指标 | 自动检查 |
|--------|----------|----------|
| 服务可独立测试 | 每服务有单测，不依赖 GUI | pytest 单测隔离（tmp/memory DB） |
| 配置驱动 | 硬编码字符串数 | `grep` 抽查 + ui_config 覆盖测试 |
| 依赖注入 | 无模块级副作用 | 测试 fixture 注入 session |
| 接口稳定 | API 契约测试 | tests/contract 13 项 |

### 3. 代码质量（自动化强制）
| 工具 | 规则 | 门槛 |
|------|------|------|
| ruff | E/F/I/W + 项目规则 | **All checks passed（0 error）** |
| ruff format | 格式统一 | already formatted |
| pytest | 全量单测 | **277 passed（不降）** |
| bandit | 安全静态扫描 | 0 高危 |
| gitleaks | 密钥泄露 | 0 命中 |
| mypy(可选) | 类型检查 | 存量误报排除后 0 error |

### 4. 运行速度（可量化性能指标）
| 场景 | 指标 | 验证方式 |
|------|------|----------|
| 桌面启动到主界面 | < 3 秒 | 启动计时脚本 |
| 学生列表首屏(2000+行) | < 200ms | 压测脚本 |
| 成绩录入保存 | < 1 秒 | 计时 |
| 报表批量生成 | 500 份 < 30 秒 | report_worker 基准 |
| 空库迁移 | < 10 秒 | alembic 计时 |
| 统计查询 | 毫秒级（读缓存） | stats API 计时 |

### 5. 个人开发者的工作方式（精简流程）
- **无签字**：每里程碑自己跑一遍质量门禁（CI 全绿 = 完成）
- **无交接**：运维手册简化为个人备忘（备份命令/数据位置）
- **无多人协作**：分支策略简化（main + feature 即可）
- **质量唯一标准**：CI 门禁 + 性能指标全绿，对你个人负责

### 6. 可维护性（易改）与可扩展性（易加）——个人作品长期价值

> 你唯一长期依赖这个系统，**改起来痛不痛、加功能快不快**比任何指标都重要。业界对应"可维护性/可扩展性"，落到本项目的具体机制：

| 场景 | 你要做的操作 | 需要的机制（已实现/已规划） | 说明 |
|------|-------------|---------------------------|------|
| **改学校名/名称/版本** | 改配置 | ✅ ui_config.json | 零代码 |
| **改菜单/页签/图标** | 改配置 | ✅ ui_config.json domains | 零代码 |
| **新增业务字段** | 配置界面操作 | ✅ FieldDefinition + ext_json | 零代码（3.7.8-3.7.12） |
| **加新表单/表格/导入导出** | 配置字段 | ✅ 动态表单/表格/导入导出联动 | 零代码 |
| **开关某功能** | 改配置 | ✅ features.json | 零代码 |
| **加新视图页** | 写视图类 + 注册 | ✅ views/registry.py | 一个文件 |
| **加新 API 路由** | 写路由 + 契约测试 | ✅ api/routes/ + TDD | 一个文件 |
| **加新报表类型** | 加模板 + 调工厂 | ✅ report_factory | 模板文件 |
| **换数据库** | 改连接串 | ✅ SQLAlchemy + alembic | 配置级 |
| **调 UI 样式** | 改主题 | ✅ theme.py + theme_manager | 令牌级 |

**可维护性量化检查（自动化）**：
| 检查 | 指标 | 方式 |
|------|------|------|
| 改动影响面小 | 新增字段零代码改动 | FieldDefinition 测试 |
| 无重复代码 | DRY | ruff + 人工抽查 |
| 命名一致 | 约定式 | ruff + 代码审阅 |
| 文档同步 | 每功能有注释/README | 提交纪律 |

**可扩展性设计原则（重构时坚持）**：
1. **配置驱动优先**：能配置的不写代码（ui_config/features/ext_json）
2. **插件式注册**：视图/路由/服务走注册表，新增不修改已有文件
3. **接口稳定**：服务层函数签名稳定，内部实现可换（报表引擎已体现）
4. **分层单向依赖**：core→api→services→repository→models，加功能只在对应层
5. **YAGNI**：不为假设需求预建（Web 前端/复杂统计已砍）

**一句话标准**：**加新功能 = 尽量零代码或一个文件；改功能 = 不动其他模块。**

---

## 变更追踪

| 日期 | 版本 | 变动 |
|------|------|------|
| 2026-08-04 | v3.0 | 从 REQUIREMENTS 整理，需求编号与任务绑定 |
| 2026-08-04 | v3.1 | 新增：开发顺序（价值/依赖/风险 + TDD 循环）、交付物清单（可运行制品+文档成套）、版本管理（SemVer + 标签化 + 自动化） |
| 2026-08-04 | v3.2 | 新增：工作接续（文档驱动五步 + 会话接续记录）、GitHub 安全与高效使用（公开仓库基线/规范化 PR 合并/免费资源表） |
| 2026-08-04 | v3.3 | 新增：数据与业务连续性（282M 生产库隔离+备份+回滚）、风险登记册（R1-R9） |
| 2026-08-04 | v3.4 | 移除多人流程（验收签字/运维交接）→ 改为个人质量门禁（结构/耦合/代码质量/性能量化指标，自动化强制）；新增可维护性/可扩展性章节 |
| 2026-08-04 | v3.5 | 新增：借鉴原则（旧项目/网上组件可参考，以本项目开发原则为主）、开发纪律（每轮≤150步/小步快进/功能复用/减代码量）、双端一致与快捷验收（M5-G1-G5）、教务审视剪裁（砍排课引擎/砍多通道推送/延后绩效） |
| 2026-08-04 | v3.6 | 第 0 条铁律（不重复造轮子：每任务先搜 GitHub/PyPI/本地资产，成品直接拿用或改造）；旧项目收尾（PR #67/#68 关闭、远端分支清理、仓库 archived 冻结、代码源头=本地 master a73a775 277 passed 零敏感） |
| 2026-08-04 | v3.7 | 新增「工程交付自动化」整体章节：PR 策略（个人也用 PR 触发 CI/squash 整洁）、CI/CD 9 jobs（补 pip-licenses 许可证检测）、生成物标准化（可复现+可溯源）、许可证合规与致谢（LICENSE/THIRD_PARTY.md/三步核验）、质量门禁（5 项 required） |

---

## 会话接续记录

| 日期 | 当前分支 | 完成到 | 下一步 | 未提交改动 |
|------|----------|--------|--------|-----------|
| 08-04 | master(旧仓 a73a775) | **M0 全部完成** | M1-1 git init + 首次 commit | 新仓未 init；~/.git 已隔离 |
| 08-04 | main(新仓) | **M1-1 完成**（git init -b main + 首次 commit f5d4082，182 文件，敏感清零前置完成） | M1-2 venv 重建中 → M1-3 冒烟 | venv 安装后台运行 |
| 08-04 | main(新仓) | **M1 全部完成**（venv 重建；277 passed；main.py --help；依赖清单补齐 factory_boy 等；data/ 目录对齐 CI） | M2-1 配置单源化（config.py → config/__init__.py） | 本次 M1 变更待 commit |
| 08-04 | main(新仓) | **M2 全部完成**（M2-1 单源化/M2-2 死代码/M2-3 /api/config/M2-4 依赖104→79/M2-5 文档/M2-6 license job；279 passed） | M3 加固验收（敏感扫描/全量测试+格式/安全扫描/空库迁移/Win dry-run） | 无 |

---

*本计划与 REFACTOR_PLAN.md（执行路线）配套：本文件=需求可追踪的主计划，REFACTOR_PLAN=按 Phase 执行的详细路线，REQUIREMENTS=需求清单源头。*